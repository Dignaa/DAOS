{
  "dependencies": {
    "@nestjs/config": "^3.3.0",
    "@nestjs/mapped-types": "*",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "typeorm": "^0.3.20"
  },
  "name": "daos",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run start:dev --prefix api\" \"npm run dev --prefix web\"",
    "format": "prettier --write '**/*.{js,jsx,ts,tsx,json,md}'"
  },
  "private": true,
  "workspaces": [
    "web",
    "api"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.0.1",
    "prettier": "^3.3.3"
  }
}
